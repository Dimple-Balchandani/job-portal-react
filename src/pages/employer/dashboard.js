import React from "react";
import Layout from '../../components/layout'

export default function Dashboard() {

    return (
        <div>
            <Layout title="Jobs">
            </Layout>
        </div>
    );
}